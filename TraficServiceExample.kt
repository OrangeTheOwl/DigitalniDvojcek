package org.example

import org.example.service.TrafficService

/*fun main() {
    val service = TrafficService()
    val trafficInfos = service.fetchTrafficInfo()
    println("Number of traffic infos: ${trafficInfos.size}")
    trafficInfos.forEach { info ->
        println("Traffic ID: ${info.id}")
        println("Description: ${info.description}")
        println("Location: ${info.location}")
        println("Date: ${info.date}")
        println("Type: ${info.type}")
        println()
    }
}*/
